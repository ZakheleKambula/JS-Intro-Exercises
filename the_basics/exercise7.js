//Exercise 7: Will an error occur if you try to access an array element with an index that is greater than or equal to the length of the array?
/*example:
let foo = ['a', 'b', 'c'];
console.log(foo.length); // => 3
console.log(foo[3]); // will this result in an error?*/
console.log("No, it returns undefined because its an element access operation on an array, and JavaScript uses undefined to represent 'no value here.'");